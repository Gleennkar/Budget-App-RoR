<div class= "nav-bar">
  <div class="back-arrow">
    <%= link_to groups_path do %>
      <i class="fa-regular fa-arrow-left">Back</i>
    <% end %>
  </div>
  <h2 class="login-head">ADD A CONTRACT</h2>
</div>
<div class="notification">
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
    <div class="signout">
        <%= button_to 'SIGN OUT', destroy_user_session_path, method: :delete %>
    </div>
</div>
<div class="container">
    <%= form_with model: contract, url: group_contracts_path(@current_group), local: true do |form| %>
        <div class="field"> 
            <%= form.text_field :name, class: 'form-control', placeholder: "Name" %>
        </div>
        <div class="field"> 
            <%= form.text_field :amount, class: 'form-control', placeholder: "Amount" %> 
        </div>
        <div class="field"> 
            <%= form.label :groups %>
            <%= form.collection_select :group_ids, Group.all, :id, :name, {}, { multiple: true, include_blank: false } %>
        </div>
        <div class="form-group form-submit">
            <%= form.submit 'Create Group', class: 'btn btn-primary' %>
        </div>
    <% end %>
</div>